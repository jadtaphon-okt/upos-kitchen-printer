import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'okt.upos.kitchen_printer',
  appName: 'UPOS Kitchen Printer',
  webDir: 'dist'
};

export default config;
